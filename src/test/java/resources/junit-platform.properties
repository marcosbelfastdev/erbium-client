# Run Cucumber with JUnit 5
cucumber.execution.parallel.enabled=true  # Enable parallel execution (optional)
cucumber.publish.enabled=false           # Disable online reporting (unless needed)
cucumber.plugin=pretty, html:target/cucumber-report.html  # Add plugins